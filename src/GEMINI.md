# Source Directory

This directory contains the application source code.

## Key Files
- `server.py`: The entry point for the MCP server. It initializes the FastMCP application and registers tools. It handles the transport mode selection (stdio vs SSE).
- `tools/`: A package containing the specific tool implementations.
